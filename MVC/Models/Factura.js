export class Factura {
    constructor(facturaId, facturaId, name, description) {
        this.facturaId = facturaId;
        this.facturaId = facturaId;
        this.name = name;
        this.description = description;
    }

    static fromJSON(json) {
    }
}
